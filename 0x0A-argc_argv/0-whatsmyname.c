#include <stdio.h>

/**
 * main - main functions
 * @argc: Argument counts
 * @argv: Argument vector
 * Return: Success
 */

int main (int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
