#ifndef BINARY_SEARCH_ALGOS
#define BINARY_SEARCH_ALGOS

#include <stdio.h>
#include <stdlib.h>

int linear_search(int *array, size_t size, int value);
int binary_search(int *array, size_t size, int value);

#endif /* BINARY_SEARCH_ALGOS */
